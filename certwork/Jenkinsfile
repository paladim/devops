pipeline {
    agent any
    stages {
        stage("Build") {
            steps {
                echo "Build start"
                sh "terraform init"
                sh "terraform plan"
            }
        }
        stage("Test") {
            steps {
                echo "Test Success!"
            }
        }
        stage("Deploy" ) {
            steps {
                echo "Deploy"
            }
        }
    }
}
